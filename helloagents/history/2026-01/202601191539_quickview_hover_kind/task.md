# 轻量迭代任务清单：精简显示设置的内部 id 默认常显（按后续需求变更）

- [√] 精简显示设置：右侧内部 id（如 `user_message`）默认常显（不再 hover/focus 才显示）
- [√] 更新知识库（CHANGELOG / Wiki）
- [√] 基础验证（`python3 -m unittest discover -s tests -p 'test_*.py' -q`）
