
# Why: Bookmark drawer 模块化（Phase41）

`ui/app/control/wire.js` 同时承担了大量“会话管理抽屉（书签/关闭监听/展示中）”的渲染与交互逻辑，导致：
- 单文件体积过大、功能域耦合高
- 后续定位/迭代成本高（容易引入回归）

本阶段将该功能域抽离到独立模块，保持行为不变，降低 `wire.js` 复杂度，为进一步拆分（长按交互 helper、渲染/动作分层）打基础。

