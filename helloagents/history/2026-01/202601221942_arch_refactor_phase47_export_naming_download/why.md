# Why - Phase47 export naming download

## 背景

`ui/app/export.js` 内同时包含导出主流程与“导出文件名生成/文件名清洗/下载触发”等通用逻辑，属于可独立复用的工具能力，导致导出模块过大、职责不够清晰。

## 目标

- 将导出文件名相关逻辑抽离到独立模块，降低 `export.js` 复杂度。
- 将下载触发逻辑抽离为可复用 helper，保持行为一致。

