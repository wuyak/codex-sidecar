# 轻量迭代任务清单：NVIDIA Model 单一下拉（与 Provider 一致）

- [√] UI：将 NVIDIA 的 `Model` 改为单一 `select` 下拉（外观/交互与 Provider 一致）
- [√] 兼容：加载配置时若当前模型不在候选列表，自动插入为“当前模型”选项，避免配置丢失
- [√] 体验：提供“自定义…”入口（prompt 输入模型 id），便于仍可使用非预设模型
- [√] 文档：同步更新 nvidia_translate 模块说明
- [√] 质量验证：UI 下拉可正常展开；保存后翻译可返回
